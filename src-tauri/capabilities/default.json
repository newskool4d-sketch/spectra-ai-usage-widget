{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "SPECTRA desktop OAuth callback and vault bridge",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default"]
}
